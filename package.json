{
  "name": "automation-team-gamma",
  "version": "1.0.0",
  "description": "Playwright UI automation for saucedemo.com",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@ndviet/adapter-playwright": "next",
    "@playwright/test": "^1.43.0",
    "typescript": "^5.4.0"
  }
}
